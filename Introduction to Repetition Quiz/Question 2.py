#stating what the subtotal is
subtotal = 0

#stating where the count starts
count = 10

#start a counting string
while count <= 50:

    #set the current subtotal
    subtotal = subtotal + count
    print (subtotal)
    count += 1